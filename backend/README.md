# Sleep Study Guard Backend

FastAPI + SQLAlchemy backend for study sessions and drowsiness detection logs.

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

The API runs on `http://localhost:8000`.
